using TMPro;
using UnityEngine;
using UnityEngine.UI;

public class MainAnatomyUI : MonoBehaviour
{
    public TMP_Text subText; 
    public Image subThumbnailImage;
    public Button button;
    int index;
    //AnatomyData data;

    //public void Init(AnatomyData _data)
    //{
    //    data = _data;
    //    subText.text = _data.mainName.Replace("_"," ");
    //    subThumbnailImage.sprite = _data.thumbnail? _data.thumbnail : null;
    //    button.onClick.RemoveAllListeners();
    //    button.onClick.AddListener(delegate { OnClick(); });
    //}
}
